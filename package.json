{
  "private": true,
  "name": "tenpest",
  "version": "0.0.0",
  "scripts": {
    "build": "el build",
    "dev": "el run",
    "deploy": "el build && cd dist/ && we deploy -p tenpest"
  },
  "devDependencies": {
    "electric-cli": "^2.2.0",
    "electric-marble-components": "^4.0.0",
    "marble": "~3.1.3",
    "marble-topbar": "^1.0.0",
    "moment": "^2.19.2"
  }
}
